# スタイル仕様

## 変更履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.1 | 2025-11-27 | 進捗バッジ用のCSS変数と進捗バッジスタイル、日付範囲スタイルを追加 |
| 1.0 | 2024-11-26 | 初版作成 |

## 1. CSS変数（カスタムプロパティ）

### 1.1 カラーパレット

```css
:root {
  /* プライマリカラー */
  --primary-color: #4f46e5;
  --primary-hover: #4338ca;

  /* セカンダリカラー */
  --secondary-color: #64748b;

  /* 状態カラー */
  --success-color: #10b981;
  --warning-color: #f59e0b;
  --danger-color: #ef4444;
  --info-color: #3b82f6;

  /* ステータスカラー */
  --todo-color: #94a3b8;
  --progress-color: #3b82f6;
  --done-color: #10b981;

  /* 進捗カラー */
  --progress-0: #94a3b8;   /* 0% - グレー */
  --progress-25: #3b82f6;  /* 25% - 水色 */
  --progress-50: #2563eb;  /* 50% - 青 */
  --progress-75: #1d4ed8;  /* 75% - 濃青 */
  --progress-100: #10b981; /* 100% - 緑 */
}
```

### 1.2 背景色

```css
:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f8fafc;
  --bg-tertiary: #f1f5f9;
}
```

### 1.3 テキストカラー

```css
:root {
  --text-primary: #1e293b;
  --text-secondary: #64748b;
  --text-light: #94a3b8;
}
```

### 1.4 ボーダー・シャドウ

```css
:root {
  /* ボーダー */
  --border-color: #e2e8f0;
  --border-radius: 12px;

  /* シャドウ */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}
```

## 2. グローバルスタイル

### 2.1 リセット

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
```

### 2.2 Body

```css
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  min-height: 100vh;
  padding: 20px;
  color: var(--text-primary);
}
```

### 2.3 コンテナ

```css
.container {
  max-width: 900px;
  margin: 0 auto;
}
```

## 3. コンポーネントスタイル

### 3.1 ヘッダー

```css
.header {
  background: var(--bg-primary);
  padding: 30px;
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-lg);
  text-align: center;
  margin-bottom: 30px;
}

.header h1 {
  font-size: 2.5rem;
  color: var(--primary-color);
  margin-bottom: 10px;
}

.subtitle {
  color: var(--text-secondary);
  font-size: 1rem;
}
```

### 3.2 統計カード

```css
.stats-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 15px;
  margin-bottom: 30px;
}

.stat-card {
  background: var(--bg-primary);
  padding: 20px;
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-md);
  text-align: center;
  transition: transform 0.2s;
}

.stat-card:hover {
  transform: translateY(-3px);
}

.stat-value {
  font-size: 2rem;
  font-weight: bold;
  color: var(--primary-color);
}

.stat-label {
  color: var(--text-secondary);
  font-size: 0.9rem;
  margin-top: 5px;
}

/* ステータス別カラー */
.stat-todo .stat-value { color: var(--todo-color); }
.stat-progress .stat-value { color: var(--progress-color); }
.stat-done .stat-value { color: var(--done-color); }
```

### 3.3 フォーム

```css
.task-form-container {
  background: var(--bg-primary);
  padding: 30px;
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-lg);
  margin-bottom: 30px;
}

.task-form-container h2 {
  color: var(--primary-color);
  margin-bottom: 20px;
  font-size: 1.5rem;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  color: var(--text-primary);
}

.required {
  color: var(--danger-color);
}

.form-group input,
.form-group textarea,
.form-group select {
  width: 100%;
  padding: 12px 16px;
  border: 2px solid var(--border-color);
  border-radius: 8px;
  font-size: 1rem;
  transition: border-color 0.2s;
  font-family: inherit;
}

.form-group input:focus,
.form-group textarea:focus,
.form-group select:focus {
  outline: none;
  border-color: var(--primary-color);
}
```

### 3.4 ボタン

```css
.btn-primary,
.btn-secondary {
  padding: 12px 24px;
  border: none;
  border-radius: 8px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}

.btn-primary {
  background: var(--primary-color);
  color: white;
}

.btn-primary:hover {
  background: var(--primary-hover);
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.btn-secondary {
  background: var(--secondary-color);
  color: white;
}

.btn-secondary:hover {
  background: #475569;
}
```

### 3.5 フィルタータブ

```css
.filter-tabs {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.filter-tab {
  padding: 10px 20px;
  background: var(--bg-primary);
  border: 2px solid var(--border-color);
  border-radius: 8px;
  cursor: pointer;
  font-weight: 600;
  transition: all 0.2s;
}

.filter-tab:hover {
  border-color: var(--primary-color);
  color: var(--primary-color);
}

.filter-tab.active {
  background: var(--primary-color);
  color: white;
  border-color: var(--primary-color);
}
```

### 3.6 タスクカード

```css
.task-list {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.task-card {
  background: var(--bg-primary);
  padding: 20px;
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-md);
  border-left: 4px solid var(--todo-color);
  transition: all 0.2s;
}

.task-card:hover {
  transform: translateX(5px);
  box-shadow: var(--shadow-lg);
}

/* ステータス別ボーダーカラー */
.task-card.status-TODO {
  border-left-color: var(--todo-color);
}

.task-card.status-IN_PROGRESS {
  border-left-color: var(--progress-color);
}

.task-card.status-DONE {
  border-left-color: var(--done-color);
  opacity: 0.8;
}

.task-header {
  display: flex;
  justify-content: space-between;
  align-items: start;
  margin-bottom: 10px;
}

.task-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--text-primary);
  flex: 1;
}

.task-card.status-DONE .task-title {
  text-decoration: line-through;
  color: var(--text-secondary);
}

.task-id {
  font-size: 0.875rem;
  color: var(--text-light);
  background: var(--bg-tertiary);
  padding: 4px 8px;
  border-radius: 4px;
}

.task-description {
  color: var(--text-secondary);
  margin-bottom: 15px;
  line-height: 1.6;
}

.task-meta {
  display: flex;
  gap: 15px;
  margin-bottom: 15px;
  flex-wrap: wrap;
}

.task-status-badge {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  padding: 6px 12px;
  border-radius: 6px;
  font-size: 0.875rem;
  font-weight: 600;
}

.task-status-badge.TODO {
  background: #f1f5f9;
  color: var(--todo-color);
}

.task-status-badge.IN_PROGRESS {
  background: #dbeafe;
  color: var(--progress-color);
}

.task-status-badge.DONE {
  background: #d1fae5;
  color: var(--done-color);
}

.task-progress-badge {
  display: inline-flex;
  align-items: center;
  padding: 6px 12px;
  border-radius: 6px;
  font-size: 0.875rem;
  font-weight: 600;
}

.task-progress-badge.progress-0 {
  background: #f1f5f9;
  color: var(--progress-0);
}

.task-progress-badge.progress-25 {
  background: #dbeafe;
  color: var(--progress-25);
}

.task-progress-badge.progress-50 {
  background: #bfdbfe;
  color: var(--progress-50);
}

.task-progress-badge.progress-75 {
  background: #93c5fd;
  color: var(--progress-75);
}

.task-progress-badge.progress-100 {
  background: #d1fae5;
  color: var(--progress-100);
}

.task-date-range {
  font-size: 0.875rem;
  color: var(--text-secondary);
  display: inline-flex;
  align-items: center;
  gap: 5px;
}

.task-date {
  font-size: 0.875rem;
  color: var(--text-light);
}

.task-actions {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.btn-action {
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  font-size: 0.875rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-edit {
  background: var(--info-color);
  color: white;
}

.btn-edit:hover {
  background: #2563eb;
}

.btn-delete {
  background: var(--danger-color);
  color: white;
}

.btn-delete:hover {
  background: #dc2626;
}

.btn-status {
  background: var(--bg-tertiary);
  color: var(--text-primary);
}

.btn-status:hover {
  background: var(--border-color);
}
```

### 3.7 モーダル

```css
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.modal-content {
  background: var(--bg-primary);
  border-radius: var(--border-radius);
  max-width: 500px;
  width: 90%;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: var(--shadow-lg);
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px;
  border-bottom: 2px solid var(--border-color);
}

.modal-header h2 {
  color: var(--primary-color);
  font-size: 1.5rem;
}

.modal-close {
  background: none;
  border: none;
  font-size: 2rem;
  color: var(--text-secondary);
  cursor: pointer;
  padding: 0;
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: background 0.2s;
}

.modal-close:hover {
  background: var(--bg-tertiary);
}

.modal-body {
  padding: 20px;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 20px;
}
```

### 3.8 トースト通知

```css
.toast {
  position: fixed;
  bottom: 30px;
  right: 30px;
  background: var(--text-primary);
  color: white;
  padding: 16px 24px;
  border-radius: 8px;
  box-shadow: var(--shadow-lg);
  opacity: 0;
  transition: opacity 0.3s;
  z-index: 2000;
}

.toast.show {
  opacity: 1;
}

.toast.success {
  background: var(--success-color);
}

.toast.error {
  background: var(--danger-color);
}
```

### 3.9 空の状態・ローディング

```css
.empty-state {
  background: var(--bg-primary);
  padding: 60px 30px;
  border-radius: var(--border-radius);
  text-align: center;
  box-shadow: var(--shadow-md);
}

.empty-icon {
  font-size: 4rem;
  margin-bottom: 20px;
}

.empty-state p {
  color: var(--text-secondary);
  font-size: 1.1rem;
  margin-bottom: 10px;
}

.empty-hint {
  color: var(--text-light);
  font-size: 0.9rem;
}

.loading {
  background: var(--bg-primary);
  padding: 40px;
  border-radius: var(--border-radius);
  text-align: center;
  box-shadow: var(--shadow-md);
}

.spinner {
  border: 4px solid var(--border-color);
  border-top: 4px solid var(--primary-color);
  border-radius: 50%;
  width: 40px;
  height: 40px;
  animation: spin 1s linear infinite;
  margin: 0 auto 20px;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
```

## 4. レスポンシブデザイン

### ブレークポイント: 768px

```css
@media (max-width: 768px) {
  .header h1 {
    font-size: 2rem;
  }

  .stats-container {
    grid-template-columns: repeat(2, 1fr);
  }

  .task-actions {
    width: 100%;
  }

  .btn-action {
    flex: 1;
  }
}
```

## 5. アニメーション

| 要素 | トリガー | 効果 |
|------|---------|------|
| 統計カード | hover | translateY(-3px) |
| タスクカード | hover | translateX(5px) |
| ボタン | hover | translateY(-2px) |
| トースト | show/hide | opacity |
| スピナー | 常時 | rotate 360deg |
